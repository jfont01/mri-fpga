Quiero que actúes como asistente técnico riguroso para una tesis de Ingeniería Biomédica sobre reconstrucción SENSE para pMRI, con validación en Python floating point y proyección a implementación posterior en FPGA. Necesito que continúes una conversación técnica previa sin perder contexto, sin inventar nada, manteniendo consistencia matemática, de notación, de shapes y de interfaz entre módulos.

REGLAS DE TRABAJO OBLIGATORIAS
1. No inventes nada. Si algo no está claro, decilo explícitamente.
2. No asumas detalles de archivos que no veas.
3. Quiero explicaciones paso a paso, con shapes explícitos.
4. Si proponés cambios de código, quiero que estén alineados con futura implementación hardware.
5. Quiero consistencia total con la formulación local de SENSE que uso.
6. Si una decisión es matemática, decilo como matemática. Si es una decisión de diseño, decilo como diseño.
7. Si analizás código, quiero que señales:
   - errores concretos,
   - inconsistencias de tipos,
   - inconsistencias de shapes,
   - y cualquier detalle que pueda romper el pipeline.
8. Quiero tono técnico, preciso y sin relleno.

1. CONTEXTO GENERAL DEL PROYECTO

Estoy desarrollando una tesis de Ingeniería Biomédica sobre reconstrucción acelerada de imágenes de RM usando SENSE para parallel MRI.

Trabajo en tres niveles:
1. generación de datos (`sense/gen`)
2. reconstrucción floating point (`sense/fp`)
3. modelo fixed-point (`sense/fxp`) con vista futura a FPGA

Además tengo una librería propia de aritmética fixed-point:
- `fxp.py`
- `cfxp.py`
- `cfxp2d.py`

El objetivo inmediato es seguir consolidando el flujo fixed-point, compararlo contra floating point y preparar el terreno para una futura implementación hardware.

2. FORMULACIÓN MATEMÁTICA LOCAL QUE QUIERO MANTENER

Estoy trabajando con:
- Af = 2
- aliasing 1D en eje y
- formulación local por bloques aliasados
- no construyo la gran matriz global explícita

SHAPES BASE
- `S.shape = (L, Nx, Ny)`  
  mapas de sensibilidad complejos

- `y.shape = (L, Nx, offset)`  
  imágenes aliasadas por bobina en representación compacta

con:
offset = Ny / Af = Ny / 2

BLOQUE LOCAL

Para cada `(nx, ny_alias)`:
- `ny0 = ny_alias`
- `ny1 = ny_alias + offset`

Defino `S_block` con columnas:
- `S[:, nx, ny0]`
- `S[:, nx, ny1]`

Luego:
- `A(nx, ny_alias) = S_block^H S_block ∈ C^{2x2}`
- `b(nx, ny_alias) = S_block^H y_block ∈ C^2`

y resuelvo:
- `A * m_hat = b`

3. SHAPES IMPORTANTES QUE QUIERO MANTENER

Para Af = 2:
- `A.shape = (2, 2, Nx, offset)`
- `b.shape = (2, Nx, offset)`

NOTACIÓN ACTUAL DE `m_hat`

Estoy migrando y quiero mantener esta convención:
- `m_hat.shape = (Af, Nx, offset)`

o sea:
- `m_hat.shape = (2, Nx, offset)`

Entonces la asignación correcta es:
`m_hat[:, nx, ny_alias] = sol`

y en `img_recon`:
- `m_hat[0, nx, ny_alias] -> ny0`
- `m_hat[1, nx, ny_alias] -> ny1`

No quiero que se vuelva a mezclar con la convención vieja `(Nx, offset, 2)` sin avisar.

4. ESTRUCTURA DE CARPETAS YA ANALIZADA

`sense/gen`
Ya se revisaron:
- `gen_phantom.py`
- `gen_smaps.py`
- `gen_coils.py`
- `gen_kspace.py`
- `gen_aliased_kspace.py`
- `gen_coil_aliased.py`
- `run_gen.sh`
- `config.conf`

`sense/fp`
Ya se revisaron:
- `compute_A.py`
- `compute_b.py`
- `compute_m_hat.py`
- `cholesky.py`
- `img_recon.py`
- `sense.py`
- `run_recon.sh`
- `config.conf`

Puntos ya fijados:
- acceso correcto a bloques:
  - `Aij = A[:, :, nx, ny_alias]`
  - `bi  = b[:, nx, ny_alias]`
- `np.linalg.solve(Aij, bi)` ya funcionó bien como referencia.

5. SOLVERS YA DISCUTIDOS

FLOATING DE REFERENCIA
La referencia numérica es:
`np.linalg.solve(Aij, bi)`

CHOLESKY LLH
También se trabajó una versión manual con:
- `A = L L^H`

LDLH
También se trabajó una versión:
- `A = L D L^H`

para evitar raíces cuadradas.

La versión `LDL^H` está pensada especialmente por interés hardware.

6. HPD / APLICABILIDAD DE CHOLESKY

Ya se estableció:
- Como `A = S^H S`, en teoría exacta `A` es Hermitiana positiva semidefinida.
- Para aplicar Cholesky estándar se necesita que sea Hermitiana positiva definida.
- Para el bloque `2x2`, una condición práctica es:
  - `A00 > 0`
  - `det(A) > 0`

También ya se discutió que esa condición puede romperse por:
- rango insuficiente de `S_block`
- mal condicionamiento
- cuantización
- saturaciones
- pérdida de hermiticidad
- errores en cast / rounding

7. MODELO FIXED-POINT ACTUAL

`fxp.py`
Tengo una clase `Fxp` basada en APyTypes / APyFixed.

Ya se trabajó en:
- `quantize(...)`
- `cast(...)`
- `to_uint()`
- `to_hex()`
- `from_uint_raw(...)`
- `div(...)`

ESTADÍSTICAS EN `Fxp`
Ya se instrumentó `FXP_STATS`, con contadores tipo:
- `fxp_add`
- `fxp_sub`
- `fxp_mul`
- `fxp_div`
- `sat`
- `underflow`

También existen o se esperan:
- `reset_fxp_stats()`
- `get_fxp_stats()`

OBSERVACIONES IMPORTANTES
- `cast()` ya fue modificado para contar saturaciones y underflow.
- Se discutió que la semántica de `round` debe ser consistente.
- Se trabajó con APyTypes como backend real para división.

`cfxp.py`
Tengo una clase `CFxp` compuesta por dos `Fxp`.

Métodos relevantes:
- `from_complex(...)`
- `from_uint_pair(...)`
- `to_complex()`
- `to_uint()`
- `to_hex()`
- `conj()`
- `cast(...)`
- `__add__`, `__sub__`, `__mul__`
- `div_by_real(...)`

Idealmente también debe poder instrumentarse con estadísticas tipo:
- `cfxp_add`
- `cfxp_sub`
- `cfxp_mul`

8. CUANTIZACIÓN DE TENSORES COMPLEJOS

Ya se definió una forma estándar de cuantizar tensores complejos 3D a `.npz` con dos tensores raw:
- `re_raw`
- `im_raw`

más metadata:
- `NB`
- `NBF`
- `signed`
- `mode`

La función de cuantización sirve tanto para:
- `S`
- `y`
- o cualquier tensor complejo 3D

CARGA CORRECTA DE `.npz`
Ya se fijó que:
- `.npy` se carga como `np.ndarray`
- `.npz` se carga como `NpzFile`

No hay que hacer `.astype(np.complex128)` directo sobre `np.load("archivo.npz")`.

9. REPORTES YA IMPLEMENTADOS O DISCUTIDOS

REPORTES DE CUANTIZACIÓN
Hay una función `write_quant_report(...)` que ya incluye o debería incluir:
- path del archivo de referencia
- path del archivo cuantizado
- shape
- dtype
- `NB`
- `NBF`
- `signed`
- `mode`
- rango representable
- errores máximos y medios
- `rmse`
- `signal_power`
- `noise_power`
- `snr_db`
- saturaciones
- peor muestra

Además se discutió agregar:
- mínimos y máximos de:
  - parte real
  - parte imaginaria
  - magnitud
para:
- datos floating
- datos cuantizados

REPORTES DE COMPARACIÓN FP VS FXP
Se trabajó con una función tipo `compare_fxp_vs_fp(...)` para comparar tensores complejos y devolver:
- `max_abs_err`
- `mean_abs_err`
- `signal_power`
- `noise_power`
- `snr_db`
- `stats`

y luego un `write_compare_report(...)` para:
- `A`
- `b`
- y potencialmente `m_hat`

10. COMPARACIÓN FLOATING VS FIXED PARA A Y b

A
Ya existen o existieron versiones tipo:
- `fp_compute_A_ij(...)`
- `fxp_compute_A_ij(...)`
- `fp_compute_A(...)`
- `fxp_compute_A(...)`

IMPORTANTE
Actualmente `fxp_compute_A(...)` global devuelve:
- `A.shape = (2,2,Nx,offset)`
- `dtype = np.complex128`

o sea: se calcula en fixed, pero luego se convierte a `np.complex128` para comparación/reportes.

ESTADÍSTICAS DEL COSTO DE A
Se midió el costo total de calcular `A` y dio:
`{'fxp_add': 9437184, 'fxp_mul': 8388608, 'fxp_sub': 1048576, 'sat': 0, 'underflow': 0}`

Eso se verificó como coherente con:
- `Nx=512`
- `Ny=512`
- `Af=2`
- `offset=256`
- `L=8`

y coincide con el costo teórico esperado de la implementación actual.

b
Ya existen o existieron versiones tipo:
- `fxp_compute_b_ij(S_q, y_q, nx, ny_alias)`
- `fxp_compute_b(S_q, y_q)`

Matemáticamente:
- `b0 = sum_l conj(s0) * y0`
- `b1 = sum_l conj(s1) * y0`

y actualmente `fxp_compute_b(...)` global también devuelve:
- `b.shape = (2, Nx, offset)`
- `dtype = np.complex128`

11. MULTIPROCESSING

Ya se implementó multiprocessing para `A` y `b`.

PARA A
Existe una versión tipo `fxp_multiprocessing_compute_A.py` con `ProcessPoolExecutor`:
- reparte por `nx`
- usa variables globales de worker:
  - `_RE_RAW`
  - `_IM_RAW`
  - `_NB_S`
  - `_NBF_S`
  - `_SIGNED`

Y devuelve:
`{"A": A, "stats": stats_total}`

PARA b
También ya existe o se generó una versión tipo `fxp_multiprocessing_compute_b.py` con la misma idea:
- reparte por `nx`
- devuelve:
`{"b": b, "stats": stats_total}`

IMPORTANTE SOBRE STATS CON MULTIPROCESSING
Ya se discutió y fijó que:
- las stats no se comparten automáticamente entre procesos
- cada worker debe:
  - resetear stats
  - calcular su parte
  - devolver stats parciales
- el padre debe acumularlas

No quiero que el asistente vuelva a asumir que las stats globales viven mágicamente compartidas entre procesos.

12. `fxp_sense.py` ACTUAL

Tengo un script `fxp_sense.py` que corre el flujo completo desarrollado hasta ahora.

Su lógica general es:
1. cargar:
   - `S_fp` desde `.npy`
   - `y_fp` desde `.npy`
   - `S_fxp` desde `.npz`
   - `y_fxp` desde `.npz`
2. calcular:
   - `A_fp = fp_compute_A(S_fp)`
   - `A_result = fxp_compute_A(S_fxp, max_workers, chunksize)`
   - `A_fxp = A_result["A"]`
   - `stats_A = A_result["stats"]`
3. comparar `A_fp` vs `A_fxp`
4. calcular:
   - `b_fp = fp_compute_b(S_fp, y_fp)`
   - `b_result = fxp_compute_b(S_fxp, y_fxp, max_workers, chunksize)`
   - `b_fxp = b_result["b"]`
   - `stats_b = b_result["stats"]`
5. comparar `b_fp` vs `b_fxp`
6. escribir un reporte global

También se discutió agregar:
- guardado de imágenes con `matplotlib`
- comparaciones visuales de `A`, `b` y potencialmente `m_hat`

13. VISUALIZACIÓN DE A Y b

Se generaron figuras comparativas de:
- magnitud
- fase
- diff

para:
- `A_fp`
- `A_fxp`
- `b_fp`
- `b_fxp`

PUNTO IMPORTANTE YA DISCUTIDO
Para diagonales `A00` y `A11`:
- deberían ser reales y no negativas
- la fase no aporta demasiado
- es mejor visualizar:
  - parte real
  - parte imaginaria
  - diff real
  - diff imag

También ya se discutió que las escalas automáticas de matplotlib pueden ser engañosas, especialmente cuando:
- `A00` y `A11` están muy cerca de 1
- o cuando la diferencia es muy chica y la fase de la diff se vuelve ruidosa

No quiero que el asistente interprete automáticamente esas imágenes como error matemático sin primero considerar el tema de escala y de fase de cantidades casi nulas.

14. ESCALA DE `y`

Se revisó un reporte de cuantización de `y_q` con `NB=32`, `NBF=31` y se observó que:
- los máximos estaban del orden de `1e-3`

Se discutió que:
- eso no implica necesariamente que `y` esté mal
- `y` no tiene por qué estar en `[0,1]` como sí podría esperarse del phantom
- la escala chica puede venir de:
  - phantom base
  - mapas de sensibilidad
  - convención FFT/IFFT
  - submuestreo
  - normalizaciones previas

Se concluyó que:
- la cuantización no parecía ser el problema
- si hay un problema de escala, viene antes en floating

15. DIVISIÓN FIXED CON APYTYPES

Se probó explícitamente la división fixed con APyTypes / `apy.fx(...)` y se confirmó que:
- la división existe y funciona
- maneja bien signos
- maneja divisores chicos positivos
- es usable para el modelo fixed

También se observó que:
- APyTypes hace crecimiento automático del formato en la división
- eso está bien como backend funcional
- pero no conviene usarlo “pelado” en el modelo final
- conviene envolverlo en `Fxp.div(...)` con:
  - `NB_out`
  - `NBF_out`
  - `mode`
  - `overflow`
  - `signed_out`

CASOS YA EVALUADOS
Se corrieron casos tipo:
- real / real positiva
- numerador muy chico
- denominador chico
- casos con signo
- complejo dividido por real positivo

Conclusión:
- APyTypes sí es suficiente como backend funcional para la división dentro del modelo fixed
- pero el formato de salida debe controlarse explícitamente

16. `Fxp.div()` Y `CFxp.div_by_real()`

Ya se propuso una implementación tipo:

`Fxp.div(...)`
Como `@classmethod` que:
1. usa la división de APyTypes
2. envuelve el resultado
3. hace `cast()` al formato elegido
4. incrementa `fxp_div`

`CFxp.div_by_real(...)`
Como división componente a componente:
- parte real / real
- parte imag / real

para usar en expresiones tipo:
- `l10 = a10 / d0`
- `z0 = y0 / d0`
- `z1 = y1 / d1`

17. SOLVER `fxp_cholesky_LDLH.py`

Ya existe o se trabajó un solver fixed-point local `LDL^H` `2x2`.

ESTRUCTURA DEL SOLVER
- `fxp_compute_LD(...)`
- `fxp_forward_subst_ldlh(...)`
- `fxp_diagonal_subst(...)`
- `fxp_backward_subst_ldlh(...)`
- `fxp_cholesky_ldlh(...)`

REPRESENTACIÓN ELEGIDA
Se eligió una representación compacta del bloque factorizado:
- `d0: Fxp`
- `d1: Fxp`
- `l10: CFxp`

en lugar de devolver matrices completas.

ESTADO ACTUAL
La estructura matemática ya se considera correcta, pero hay una salvedad muy importante:

INTERFAZ DE ENTRADA
Este solver no trabaja sobre:
- `Aij` y `bij` en `np.complex128`

sino sobre:
- `Aij` con entradas `Fxp/CFxp`
- `bij` con entradas `CFxp`

Entonces:
- no se puede enchufar directamente a `fxp_compute_A(...)` y `fxp_compute_b(...)` globales actuales
- porque esas ramas globales convierten todo a `np.complex128`

Esto es una pieza crítica del contexto.

18. `fxp_compute_m_hat.py`

Ya se propuso o se discutió una primera versión de `fxp_compute_m_hat.py` que:
- recibe:
  - `A.shape = (2,2,Nx,offset)` en `np.complex128`
  - `b.shape = (2,Nx,offset)` en `np.complex128`
- devuelve:
  - `m_hat.shape = (2,Nx,offset)` en `np.complex128`

y permite resolver con:
- `numpy-linalg-solve`
- `numpy-linalg-cholesky`
- `manual-cholesky` con:
  - `llh`
  - `ldlh`

IMPORTANTE
Esta versión de `fxp_compute_m_hat.py`:
- todavía no es solver fixed puro
- resuelve sobre `A_fxp` y `b_fxp` ya convertidos a `np.complex128`

O sea, sirve para estudiar:
- el efecto de cuantizar `A` y `b`
- y cómo eso impacta en `m_hat`

pero no es todavía una resolución íntegramente fixed con `Fxp/CFxp`.

19. QUÉ QUIERO HACER A PARTIR DE ACÁ

Quiero continuar desde este estado sin que se me vuelva a explicar todo desde cero.

PRIORIDADES ACTUALES
1. Seguir consolidando la rama fixed-point.
2. Revisar consistencia entre:
   - `fxp_compute_A_ij`
   - `fxp_compute_A`
   - `fxp_compute_b_ij`
   - `fxp_compute_b`
   - `fxp_compute_m_hat`
   - `fxp_cholesky_LDLH`
3. Asegurar consistencia de:
   - formatos
   - crecimiento de palabra
   - cast
   - saturaciones
   - underflow
   - interfaces
4. Mantener todo alineado con futura implementación hardware.

LO QUE QUIERO DEL NUEVO CHAT
Quiero que:
1. leas este contexto y lo asumas como base
2. no reabras decisiones ya tomadas sin motivo
3. señales claramente:
   - si algo está bien
   - si algo no está cerrado
   - si algo depende de una decisión de diseño
4. mantengas consistencia de shapes y notación

QUÉ NO QUIERO
- No quiero respuestas genéricas.
- No quiero que vuelvas a asumir `m_hat.shape = (Nx, offset, 2)` sin avisar.
- No quiero que confundas solver fixed puro con solver sobre `np.complex128` cuantizado.
- No quiero que hables de hardware como si el costo bruto de operaciones alcanzara para decidir eficiencia sin considerar paralelismo, pipeline y memoria.

QUÉ ME GUSTARÍA QUE HAGAS PRIMERO AL RETOMAR
Cuando retomemos, quiero que primero:
1. me digas qué módulo o función concreta vas a revisar primero
2. y luego sigas directamente desde ahí

GUÍA RÁPIDA DE TÉRMINOS
- `fp` = floating point puro
- `fxp` = modelo fixed-point en Python
- `A_fxp`, `b_fxp` = actualmente tensores cuantizados pero convertidos a `np.complex128`
- `solver fixed puro` = solver que opera con `Fxp/CFxp`, no con `np.complex128`

Fin del prompt.





sense/
├─ fp/
│  ├─ fp_compute_A.py
│  ├─ fp_compute_b.py
│  ├─ fp_compute_m_hat.py
│  ├─ fp_img_recon.py
│  ├─ fp_sense.py
│  └─ run_sense_fp.sh
│
├─ fxp/
│  ├─ helpers/
│  │  ├─ comparison.py
│  │  ├─ img_savers.py
│  │  └─ rpt_writer.py
│  │
│  ├─ multiprocessing/
│  │  ├─ fxp_multiprocessing_compute_A.py
│  │  └─ fxp_multiprocessing_compute_b.py
│  │
│  ├─ quantizer/
│  │  ├─ helpers.py
│  │  ├─ quantize_complex_tensor_3d.py
│  │  ├─ quantizer.py
│  │  └─ run_quantizer.sh
│  │
│  ├─ solvers/
│  │  ├─ fxp_cholesky_LDLH.py
│  │  ├─ fxp_compute_A.py
│  │  ├─ fxp_compute_b.py
│  │  ├─ fxp_compute_m_hat.py
│  │  ├─ fxp_sense.py
│  │  └─ run_sense_fxp.sh
│  │
│  └─ output/
│
├─ gen/
│  ├─ output/
│  ├─ gen_aliased_kspace.py
│  ├─ gen_coil_aliased.py
│  ├─ gen_coils.py
│  ├─ gen_kspace.py
│  ├─ gen_phantom.py
│  ├─ gen_smaps.py
│  └─ run_gen.sh
│
├─ global_config.conf
└─ run.sh